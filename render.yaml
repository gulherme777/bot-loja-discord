services:
  - type: web
    name: g7-store-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: MP_ACCESS_TOKEN
        sync: false
      - key: PORT
        value: 10000
